# Engineering Provenance

F001 → `ResourceState`, lines 7–10. F003 → `LoadPredictor.predict`, lines 22–29. F004 → `match_score` and `select_node`, lines 32–45. These internal paths are not specification support.
