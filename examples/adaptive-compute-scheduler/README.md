# Adaptive Compute Scheduler

Synthetic, non-sensitive demo for patent-skill. It samples live node resources, predicts near-future load from recent history, combines current and predicted state into a match score, and selects a target node.
