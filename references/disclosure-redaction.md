# Disclosure and redaction

Default source paths, line numbers, internal URLs, customer names, private datasets, secrets, and irrelevant architecture details to `internal-only`. Export only material explicitly classified `filing-disclosable`. Rendering must strip internal HTML annotations.
